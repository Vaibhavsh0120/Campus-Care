{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "if [ -d flutter ]; then cd flutter && git fetch --depth 1 origin 3.29.3 && git checkout 3.29.3 && cd ..; else git clone --depth 1 --branch 3.29.3 https://github.com/flutter/flutter.git flutter; fi && ./flutter/bin/flutter config --enable-web && ./flutter/bin/flutter pub get",
  "buildCommand": "./flutter/bin/flutter build web --release",
  "outputDirectory": "build/web"
}
